---
id: 769
title: OpenAPI Operation
state: reviewing
phase: 1
---

# [ADE-769] OpenAPI Operation

This ADE outlines principles for designing OpenAPI operations (paths).

## Guidance

### Response Codes

- Each operation MUST specify at least one 2xx successful response.
- Operations SHOULD include appropriate error responses (4xx and 5xx).
- Use standard HTTP status codes as defined in [ADE-200: HTTP Status Codes](200.md).

### Operation ID

- Each operation MUST have a unique `operationId`.
  - Follow the guidelines in [ADE-755: OpenAPI operationId](755.md) for naming conventions.

### API Audience

- Each operation MUST specify its API audience in `x-audience`.
  - See [ADE-752: OpenAPI audience](752.md) for more information.

### Parameters

- Define all parameters using the `parameters` array at the operation level.
- Use `$ref` to reference common parameters defined in the `components` section.
  - Follow [ADE-758: OpenAPI $ref](758.md) for best practices on using references.

### Request Body

- For operations that accept a request body (e.g., POST, PUT, PATCH), define the `requestBody` object.
- Specify the content type(s) supported (e.g., `application/json`).
- Use `$ref` to reference request schemas defined in the `components` section.

### Responses

- Define responses for all possible status codes the operation may return.
- Use `$ref` to reference response schemas defined in the `components` section.
- Include examples where appropriate, following [ADE-756: OpenAPI Provide Examples](756.md).

### Security

- Apply security requirements at the operation level if they differ from the global security scheme.
- Reference security schemes defined in the `components/securitySchemes` section.

### Description

- SHOULD Provide a clear and concise description for each operation.
- Include any important notes or caveats about the operation's behavior.
- [Must document implicit resource filtering](763)

### Deprecation/Sunset

- Mark deprecated operations using the `deprecated: true` flag.
- Include information about the deprecation in the operation's description, such as the reason for deprecation and suggested alternatives.
- SHOULD reference pre-defined shared headers(See [ADE-767: Shared common headers](767)) for deprecation or components.headers(if you would not like to reference the external public header definitions yaml) to avoid redundant definitions.
- SHOULD add sunset related descriptions since only adding Sunset header is not clear enough.

### Rate Limiting

- SHOULD include rate limiting information using `x-rate-limit` extension.
- Follow [ADE-753: OpenAPI x-rate-limit](753.md) for implementation details.
- SHOULD reference pre-defined shared headers(See [ADE-767: Shared common headers](767)) for rate limiting or components.headers(if you would not like to reference the external public header definitions yaml) to avoid redundant definitions.

### Extensible Enums

- For open-ended enumerations, SHOULD use `x-extensible-enum` instead of `enum`.
- Follow [ADE-768: Extensible Enums](768.md) for implementation guidelines.

## Example

Here's an example of a well-defined operation in OpenAPI 3.1.0:

```yaml
components:
  headers:
    ETag:
      $ref: 'https://alivedise.github.io/api-design-essentials/models/headers-1.0.0.yaml#/ETag'
  parameters:
    CommonHeader:
      name: Common-Header
      in: header
      required: true
      schema:
        type: string
      description: This is an example header parameter
paths:
  /example:
    get:
      summary: Example endpoint
      parameters:
        - name: Example-Header
          in: header
          required: true
          schema:
            type: string
          description: This is an example header parameter
        - $ref: '#/components/parameters/CommonHeader'
  /users:
    post:
      operationId: createUser
      summary: Create a new user
      description: Creates a new user account with the provided information.
      tags:
        - users
      x-audience: public
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/User'
      responses:
        '201':
          description: Successful response
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      security:
        - bearerAuth: []
      x-rate-limit:
        rate: 100
        burst: 200
        unit: minute
```

## Related ADEs

- [ADE-200: HTTP Status Codes](200.md)
- [ADE-752: OpenAPI audience](752.md)
- [ADE-753: OpenAPI x-rate-limit](753.md)
- [ADE-755: OpenAPI operationId](755.md)
- [ADE-756: OpenAPI Provide Examples](756.md)
- [ADE-758: OpenAPI $ref](758.md)
- [ADE-768: Extensible Enums](768.md)
- [ADE-763: OpenAPI implicit resource indication](763)

## References

- [OpenAPI Specification](https://spec.openapis.org/oas/latest.html)

## Changelog

- `2025.03.07`: Add link to implicit resource filtering because the description field is under the operation object.
