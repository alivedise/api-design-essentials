---
id: 361
title: HTTP Caching
---
# [ADP-361] HTTP Caching

## Overview

This document outlines the guidelines and best practices for implementing HTTP caching in a RESTful API to improve performance, reduce latency, and decrease load on the server.

## Guidance

## Background Knowledges

### HTTP Caching Headers

HTTP caching is controlled through the use of specific headers in HTTP requests and responses. The primary headers used for caching are:

- **Cache-Control**
- **ETag**
- **Last-Modified**
- **Expires**

## Implementation Detail

### Server-Side Implementation

1. **Generate ETags**: Ensure that each resource has a unique ETag and include it in the response header.
2. **Set Cache-Control Directives**: Determine the appropriate caching policy for each endpoint and include the `Cache-Control` header.
3. **Include Last-Modified**: Set the `Last-Modified` header to the last modification date of the resource.
4. **Handle Conditional Requests**: Implement logic to handle `If-None-Match` and `If-Modified-Since` headers and return `304 Not Modified` status if the resource has not changed.

### Client-Side Implementation

1. **Store Cache Headers**: Store `ETag`, `Last-Modified`, and other relevant headers along with the cached response.
2. **Include Conditional Headers**: When making a request, include `If-None-Match` and `If-Modified-Since` headers to validate the cache.
3. **Handle 304 Responses**: Update the cache if a `304 Not Modified` status is received, maintaining the same response data.

### Example Endpoint

```json
GET /api/resource/123 HTTP/1.1
Host: example.com
If-None-Match: "abc123"
If-Modified-Since: Tue, 20 Jul 2021 19:30:00 GMT
```

```json
HTTP/1.1 200 OK
Cache-Control: public, max-age=3600, must-revalidate
ETag: "abc123"
Last-Modified: Tue, 20 Jul 2021 19:30:00 GMT
Expires: Wed, 21 Jul 2021 19:30:00 GMT
Content-Type: application/json

{
  "id": 123,
  "name": "Resource Name",
  "updated_at": "2021-07-20T19:30:00Z"
}
```

### Best Practices

1. **Define Caching Policies**: Clearly define and document the caching policies for each API endpoint.
2. **Minimize Cache Invalidation**: Design resources and endpoints to minimize the need for cache invalidation.
3. **Use Appropriate Cache Directives**: Select appropriate `Cache-Control` directives based on the nature of the data and usage patterns.
4. **Monitor and Adjust**: Continuously monitor cache performance and adjust headers and policies as necessary.

By following these guidelines, you can effectively implement HTTP caching in your RESTful API, resulting in improved performance and user experience.

## Reference

- https://www.rfc-editor.org/rfc/rfc9110
- https://datatracker.ietf.org/doc/html/rfc7234
