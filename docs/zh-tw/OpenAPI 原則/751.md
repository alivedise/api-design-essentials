---
id: 751
title: 新的 OpenAPI 文件
state: reviewing
phase: 1
---
# [ADE-751] 新的 OpenAPI 文件

## 指導

- 必須(MUST)使用 OpenAPI 3.1.0 或更高版本

  ```yaml
  openapi: 3.1.0
  ```

- 必須(MUST)使用 YAML 格式以提高可讀性和便於拆分
  - JSON 格式會導致深層嵌套的對象，難以閱讀和維護

- 必須(MUST)將所有回應架構移至 `components` 部分
  - 詳見 [ADE-758: OpenAPI 中 $ref 的使用](758.md)

- 必須(MUST)在 `info` 部分包含以下元數據:
  - `title`: API 名稱
  - `description`: API 目的的簡要概述
  - `version`: 遵循 [ADE-24: 版本控制機制](24.md) 的 API 版本
  - `contact`: API 支持資訊 (參見 [ADE-759: OpenAPI 聯繫資訊](759.md))

    示例:

    ```yaml
    info:
      title: 用戶管理 API
      description: 用於管理用戶帳戶和個人資料的 API
      version: 1.0.0
      contact:
        name: API 支持團隊
        url: https://example.com/support
        email: api-support@example.com
    ```

- 應該(SHOULD)在 `components` 部分定義安全方案
- 必須(MUST)對操作或整個 API 應用適當的安全要求

  示例:

  ```yaml
  components:
    securitySchemes:
      bearerAuth:
        type: http
        scheme: bearer
        bearerFormat: JWT

  security:
    - bearerAuth: []
  ```

- 應該使用標籤來分組相關操作
- 必須為所有標籤提供描述

  示例:

  ```yaml
  tags:
    - name: users
      description: 用戶管理操作
    - name: auth
      description: 認證和授權操作
  ```

- 必須使用 kebab-case 命名路徑段
- 必須使用 camelCase 命名查詢參數和操作 ID
- 應該為每個操作提供摘要和描述
- 必須為操作使用適當的 HTTP 方法 (參見 [ADE-110: HTTP 方法](110.md))

  示例:

  ```yaml
  paths:
    /users/{userId}:
      get:
        summary: 獲取用戶詳情
        description: 檢索特定用戶的詳細資訊
        operationId: getUserDetails
        tags:
          - users
        parameters:
          - name: userId
            in: path
            required: true
            schema:
              type: string
        responses:
          '200':
            description: 成功回應
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/User'
  ```

- 應該為請求體和回應提供示例 (參見 [ADE-756: OpenAPI 提供示例](756.md))
- 必須在 `components` 部分定義常見錯誤回應
- 應該使用標準 HTTP 狀態碼 (參見 [ADE-138: HTTP 狀態碼](138.md))
- 必須遵循問題詳情格式進行錯誤回應 (參見 [ADE-401: HTTP 問題基礎](401.md))
- 如果 API 支持 webhooks，應該定義它們 (參見 [ADE-760: OpenAPI Webhooks](760.md))
- 應該(SHOULD)對您的 OpenAPI 文件進行版本控制。
- [DRAFT] 對 API 定義的更新必須(MUST)需要代碼審查，並且應該取決於服務更新的整體架構審查。

## 關聯 ADEs

- [ADE-24: Versioning Mechanism](24.md)
- [ADE-41: API Versioning](41.md)
- [ADE-110: HTTP Methods](110.md)
- [ADE-138: HTTP Status Code](138.md)
- [ADE-401: HTTP Problem Basics](401.md)
- [ADE-753: OpenAPI x-rate-limit](753.md)
- [ADE-756: OpenAPI Provide Examples](756.md)
- [ADE-758: OpenAPI $ref](758.md)
- [ADE-759: OpenAPI Contact Info](759.md)
- [ADE-760: OpenAPI Webhooks](760.md)

## 參考

- [OpenAPI Specification](https://spec.openapis.org/oas/latest.html)
- [OpenAPI Best Practices](https://oai.github.io/Documentation/best-practices.html)
- [IBM API Handbook](https://cloud.ibm.com/docs/api-handbook?topic=api-handbook-design-methodology)

## Changelog

- `2024.10.17` add version control
